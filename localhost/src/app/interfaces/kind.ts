import {Image} from './image';

export interface Kind {
  displayLink: string;
  htmlSnippet: string;
  htmlTitle: string;
  image: Image;
  kind: string;
  link: string;
  mime: string;
  snippet: string;
  title: string;
}
